# 1 - List
# a

my_list = list(range(100))
print(my_list)
print(type(my_list))

# b

myList = [0, 1, 2, 3, 4, 5, 6, 7, 8, 8, 9]
print(myList[-3:] + myList[:-3])

# C 
# The answer to this is 3, the reason is the first part len([[1,2]]) is a list, that in it has a list. so it would cound 1, for one list. 
# it doesn't matter what is the the list, that's in the list. It would then take 1 * 3, which is 3. 

# d 

my_list_ten = [1, 1, 2, 3, 4, 4, 5, 6, 7, 8]
my_list_ten_mem = []

for item in my_list_ten:
    my_list_ten_mem.append(id(item))
    
print(my_list_ten_mem)

# e 
del my_list_ten

# f

my_list_ten = [1, 1, 2, 3, 4, 4, 5, 6, 7, 8]
my_list_ten_mem = []
my_new_list = [1, 1, 2, 3, 4, 4, 5, 6, 7, 8]
my_new_list_mem = []

for item in my_list_ten:
    my_list_ten_mem.append(id(item))
    

for item in my_new_list:
    my_new_list_mem.append(id(item))
    
print(my_list_ten_mem)
print(id(my_new_list_mem))
## This prints the same naddress numbers because like the past asisgnment with location of a single letter, like "a" it points to the same location instead of doubling it up.

# g 

# my guess is  changing the information stored at that id() location in memory, which changed the list once it was called.
# We need to change the deep layer, using deep.copy 

import copy

x = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
y = copy.deepcopy(x)
y[0][0] = 999
print(x)
print(y)

# I had to import copy, cause deep.copy errord on me. 

# h

# I first thought no, but I read online yes you can. I practised with a loop inside a list, and it worked! so I guess that's coming next.

# i 
number_spaces = 0
phrase = str("To be, or not to be, this is the question") 

for char in phrase:
  if char in " ":
    number_spaces = number_spaces + 1

print(number_spaces)

# j 
# I have picked, list.insert, list.sort, list.copy, list.remove, and list.pop

# Challenged

#I had a lot of challenges with this one, I had to do most of these in thonny several times. 
# for loops ending up putting me for a loop, and took some work to grasp. 
# Having some knowledge of R, what I was doing made sense as loops are a big part of R. 
# Learning the python way was a bit of a challenge, but glad I did it. 
