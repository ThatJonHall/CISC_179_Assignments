# Exceptions
#part 1

|Situation         | Bad input or bug? | Reasoning 
|The program expects an integer but the user enters 'hello' | Bad input | It was coding for an input, but wans't properly set to handle non integers
|The program write 'prin()' instead of print | Bug | A simple mis type of the keyboard, a spelling error
|The program divides by a value entered by the user, and the unser enters '0' | Bug | the program wasn't set to handle ZeroDivisionError

#part 2 

#1 - input. That's the first thing that happens when a "bad" input is made.
#2 int() Is designed to tahe a string that prepresents a whole number. Anything that's not a whole number be fail with this is performed.
#3 two possible errors, ValueError when the input can't be converted to a whole number, or ZeroDivisionError, when a zero is entered and that causes a math error.
#4 if there's a zero entered it would be a ZeroDivisionError, casuse you can't divide by 0 in math you get "undefined"

#part 3

try:
    value = int(input("Enter an integer: "))
    print("You entered:", value)
    # risky code
except ValueError:
    print("That's not a natural number, it's unnatural!")
    # what to do if that specific error happens

#part 4 
|input          | lines / branches executed         | Lines Skipped |    Exception
|2              | All the try and Print("D') will run | Both exceptions will be skipped | No exceptions
|0              | Print a and b will print, after input is 0 it will print ZeroDividionError except line print("D")| Zero DivisionError 
|Hello          |A will print, input will fail not print, only ValueError excpt line + print("D"). | ValueError exption
